# Device Configuration
device {
    mac_address = "XX:XX:XX:XX:XX:XX"
    name = "My Device"
    enabled = true
}

device {
    mac_address = "XX:XX:XX:XX:XX:XX"
    name = "My Watch"
    enabled = true
}

# Threshold Settings
thresholds {
    lock_threshold = -25
    unlock_threshold = -15
}

# Timing Configuration
timings {
    lock_hold_seconds = 3
    unlock_hold_seconds = 3
    poll_interval = 1
}

# Network Settings
network {
    enabled = true
    service_name = "hyprproxlock"
    port = 5678
}